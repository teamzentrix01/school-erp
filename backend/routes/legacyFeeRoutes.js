const r=require("express").Router(),c=require("../controllers/legacyFeeController"),{protect,authorizeRoles}=require("../middleware/authMiddleware");r.use(protect,authorizeRoles("admin","accounts"));r.get("/",c.list);r.post("/",c.create);r.get("/:id",c.getOne);r.post("/:id/payments",c.pay);r.put("/payments/:paymentId/reverse",c.reverse);r.get("/payments/:paymentId/receipt",c.receipt);module.exports=r;
